# 專案執行規則

- 網誌動線改善依 `docs/personal-navigation-plan.md` 與 `docs/personal-navigation-progress.md` 接續；規劃已於 2026-09-14 獲使用者確認。每次限一個可保存、可驗證的小步驟，完成後記錄狀態。
- 使用者要求保留至少 10% 額度供日常使用。開始、每個小步驟結束、啟動長任務或代理前，讀取所有適用額度視窗；以剩餘最低者判斷。讀不到額度時不開始長任務。
- 任一適用視窗剩餘不高於 15% 時，停止新增實作或代理工作並保存交接，提前留下緩衝；不高於 10% 時停止專案工作。消耗難以控制時更早收尾。這是操作停止規則，不能宣稱已在帳號硬鎖額度，也不能保證其他任務不消耗同一帳號。
- 每次交付記錄開始／結束餘額、完成項目、實際驗證及下一步；未驗證不標完成。移交代理時附上同樣限制。
- 保留作者原有網站用途、文章與網址、以及進場前未提交修改。部署需有明確授權。
- 維護 `source/microblog.json` 時保留每筆既有 `id`，不隨改文或排序重算。新增紀錄後先執行 `node tools/assign-microblog-ids.js` 預覽，再以 `--write` 補齊新 ID；此工具保留既有欄位與順序。建置若報缺失或重複 ID，先修正來源，不能改成每次建置臨時產生。
