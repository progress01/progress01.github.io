# 「從實際抽象功能」文章進度

## 2026-09-17｜完成第一步：從對話拆成七篇公開工作知識文章

- 開始額度：5 小時視窗剩 94%、每週視窗剩 71%。
- 改成公開文章前額度：5 小時視窗剩 93%、每週視窗剩 70%。
- 收尾額度：5 小時視窗剩 92%、每週視窗剩 70%。
- 完成：將 `C:\Users\111\Desktop\從實際抽象功能.md` 的思考脈絡改寫成 7 篇文章，放在 `source/_posts/實驗室/`，不保留 `you asked / chatgpt response` 對話格式；原始桌面檔未修改。
- 範圍：需求抽象、逐字稿轉判斷模型、執行與審查的認知落差、風險式補審、可遷移能力、AI 雙軌工作法、PM／SA 基礎學習地圖。
- 實際驗證：7 篇 `node tools/excerpt-check.js --post ...` 全部 PASS；`npm run verify` 通過，69 項回歸測試、990 個生成檔案、368 個 HTML、48,796 個 href/src 均通過；7 個 `/work/` 頁面與工作知識首頁卡片已生成。
- 標籤清單：同步 `docs/tag-reorganization-review.json`，公開文章清單通過（264 篇）。
- 發布狀態：只完成本地公開文章來源與本地建置，未執行 `npm run deploy`。
- 下一步：逐篇補入可核對的案例證據與文章間的延伸閱讀；若要部署，另行明確授權。

## 2026-09-17｜系列標題辨識修正

- 開始／收尾額度：5 小時視窗剩 91%、每週視窗剩 70%。
- 完成：7 篇標題統一為「工作知識｜從實際抽象功能系列 01～07｜篇名」，並把正文與更新紀錄中的「草稿」改為公開文章語境。
- 保留：所有 permalink、文章日期、分類、標籤與工作知識封面設定不變。
- 實際驗證：7 篇摘要檢查 PASS；`npm run verify` 通過，69 項測試、990 個生成檔案、368 個 HTML、48,796 個 href/src 通過；首頁與工作知識分類頁均顯示系列編號。
- 下一步：如需更明確的系列導覽，可再建立系列總覽頁與文章間索引；本次先不擴大範圍。

## 2026-09-17｜工作知識加入草稿夾熱力圖

- 開始／收尾額度：5 小時視窗剩 90%、每週視窗剩 70%。
- 完成：在 `source/reading-desk.yml` 新增 `B-02`「從實際抽象功能｜工作知識系列」，登錄 7 篇公開文章；每篇保留實際 `/work/` 網址，狀態標為 `learning`，明確表示仍未完稿。
- 日期語意：7 篇以 `2026-09-17` 作為加入草稿夾的日期，因此會在該日的熱力圖集中顯示 7 筆整理紀錄；之後不會每天重複計數。
- 相容性：草稿夾驗證器原本只接受 `/learning/`，已擴充為同時接受 `/learning/` 與 `/work/`，並新增回歸測試。
- 實際驗證：`node --test tools/tests/content-check.test.js` 5 項通過；`npm run verify` 通過，70 項測試、990 個生成檔案、368 個 HTML、48,796 個 href/src 通過；生成的 `public/reading-desk.json` 確認含 7 筆工作知識項目。
- 發布狀態：未執行部署；文章仍可繼續修改，草稿夾會保留這批尚未完稿紀錄。
- 下一步：逐篇補案例、證據與文章互鏈，完成後再把各項狀態調整為 `published` 或補上完成日期。
