# 網誌第四輪稽核與修正

日期：2026-09-11。由主代理檢查與驗收，LUNA 修改文章、工具及技能。本輪承接第三輪清單，處理歌曲封面與 Blogger 維護工具的修改範圍。

## 已完成的內容修正

- 109 篇歌曲推薦補上 `cover`，採用該篇正文與音樂圖牆已共用的本機 WebP。每篇原始副本與選圖依據保存在 `tmp/round4-covers-before/`。
- 逐篇比對確認只有新增一行 cover；正文、日期、標題、標籤、永久連結與換行格式均保留。
- 修正文章模板：是否隱藏列表摘要，改由是否已輸出特色卡片決定。歌曲補上 cover 後仍顯示原本的列表摘要；學習文章的特色卡片不重複摘要。
- 109 篇生成歌曲頁的 BlogPosting 圖片均對應正文圖片。109 個文章頁與 109 個列表項目，共 218 筆，文字、圖片來源及連結與修正前一致。
- 其餘 704 個來源檔案的 SHA-256 完全一致，檔案集合也沒有增減；包含圖牆、圖片資產、非歌曲文章及資料檔。

## Blogger 工具修改

四個維護工具統一支援可重複的 `--post`，整站操作則需明確使用 `--all`。兩者互斥，缺少範圍的 `--write` 會拒絕。省略 `--write` 僅預覽，restore 預覽不下載圖片。

- `restore-blogger-images.js`：指定文章才讀取該篇；拒絕選中 feed URL 的歧義；修正第 1 與第 10 張圖片標記互相誤替換；檢查輸出父目錄的實際路徑，防止 junction 導向專案外。
- `normalize-blogger-imports.py`：使用真實 YAML 解析分類；支援全形、半形原文標記；顯式指定的文章若包含不適格項目，整批拒絕；先計畫後寫入。
- `convert-images-to-webp.py`：單篇模式只處理選中文章引用的圖片與文字；支援空白、括號及 URL 編碼；保留原始 PNG/JPG；預檢所有圖片與文字計畫，避免後續壞圖造成前段先被修改；保護既有內容不同的 WebP。
- `build-blogger-photo-wall.py`：單篇模式維護對應卡片與縮圖，保留未選內容；圖片的第一個檔名不必是 `01`，normalizer 與圖牆使用相同縮圖名稱。特殊網址、換行、歧義卡片及寫入前計畫另有回歸測試。

操作方式與 Python/Pillow 前置需求見 [Blogger 工具操作](tools/BLOGGER-TOOLS.md)。Python 維護工具測試採獨立 `npm run test:blogger`，一般網站 verify 不新增 Python 依賴。

## SKILL 更新

已安裝更新 `blogger-import-update` 與 `song-blog-update`。兩份技能保留既有寫作、日期、分類與發布規則，將舊的全站維護命令改成指定文章流程。

明確記載 restore 會從 feed 重建選定文章正文；它不能當作保留本地正文修改的無損補圖命令。匯入歌曲有可用圖片時，也要檢查並補上 cover。

暫存版與已安裝版均使用正式 `quick_validate.py` 搭配真實 PyYAML 6.0.3 驗證通過，未使用替代 parser 或 shim。原版備份位於 `C:\Users\111\AppData\Local\Temp\codex-blog-skills-round4-backup-046b9c775d0d4b49832f359fee3fd300`。

## 驗收紀錄

- `npm run verify` 通過：32 項 Node 回歸測試、937 個生成檔案、327 個 HTML、32,036 個 href/src。
- 現有 91 篇 Blogger 圖牆文章的預計網址均能對應生成頁。
- 主代理實際執行指定一篇既有文章的三個 Python dry run，均回報零變更；沒有對正式文章或圖片執行 Blogger 工具的寫入流程。
- 主代理另外測試轉圖工具：第二張圖片損壞、文字計畫失敗均零寫入；重複轉換保留檔案內容與修改時間。
- `npm run test:blogger` 通過：13 項 Python 工具測試。涵蓋單篇範圍、混合不適格文章拒絕、圖牆 CRLF 保留、含 `&` 與百分比編碼的永久連結、重跑不重複卡片，以及錯誤標記／缺漏圖片在寫入前拒絕。
- 圖牆預覽與寫入共用同一份變更計畫；`git diff --check` 通過，測試產生的 bytecode 已清理，原本受版本控制的 bytecode 已還原。

## 尚未處理的項目

第三輪列出的未註冊技術標籤仍保留警告，尚未擴充標籤分類。這份紀錄只代表本輪範圍的修正與驗收，不表示整站所有潛在問題已排除。

本輪尚未 commit、push 或部署；線上網站不會因這次本機修正而自動更新。
