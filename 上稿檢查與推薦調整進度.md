# 上稿檢查與 RSS 調整進度

2026-09-12：LUNA 額度恢復後完成實作，由統籌者審查、測試與安裝。本輪只完成 RSS 視覺及上稿檢查，延伸閱讀相關性改善暫緩。未部署、提交或推送。

## RSS 視覺

- 修改主題 post-followme.njk 與 post-followme.styl。只有單一 RSS 頻道時採用精簡橫列；其他頻道保留原版型。
- 文字 14px，點擊高度約 38px，訂閱 URL 保持 /atom.xml。
- N+4 瀏覽器實測：原高 164px；桌機與 390px 手機縮為 54.375px；320px 手機換行為 81.969px，無橫向溢出。

## 上稿檢查

- 新增唯讀 tools/excerpt-check.js，指令 npm run check:excerpt -- --post "source/_posts/<path>.md"；重複 --post 可檢查多篇。
- 明確 --all 才全站稽核，不能與 --post 混用；不將全站摘要限制接入日常 verify 阻擋其他更新。
- 以 N+4 引言為依據，初始門檻為 86 個可見非空白 Unicode 字元，包含標點；超過為 FAIL，不能取代實際卡片驗收。
- Markdown 渲染後辨識 more marker，檢查字數、段落與結構；程式碼中的假 marker 不當成分隔。
- front matter excerpt／description 另報字數及覆蓋呈現風險；歌曲固定 HTML 卡片與單封面保留，要求核對生成卡片。
- PASS 代表沒有靜態警告；WARN 需人工核對卡片；FAIL／ERROR 為非零退出。刻意長摘要可報例外，不可假稱 PASS。
- 不自動截文、不刪正文、不更動作者輸入日期；純排版調整不刷新 updated。
- 新 skill 已安裝於 C:/Users/111/.codex/skills/blog-excerpt-check/。
- 已銜接 song-blog-update、blogger-import-update、n-plus-days-blog-update、reading-desk-update、work-knowledge-blog-update，每篇新增或修改文章都執行目標檢查；index-only JSON/YAML 更新除外。
- N+days 舊「三段、120–160 字」矛盾已移除，改引用一段短引言及共用標準。
- 安裝前核對五份既有 SKILL SHA-256，只更新 SKILL.md，保留其他資源。

## 驗證

- 新檢查器 10 項測試通過；全部回歸測試 54 項通過。
- 六份 SKILL 使用真正 PyYAML 與官方 quick_validate.py 驗證通過。
- N+4 實際檢查 PASS，visible=86/86，單段、單一 marker。
- clean/build 通過；網站檢查通過 329 個 HTML、32,258 個 href/src，包含 JSON-LD 及資料連結。
- 內容檢查通過；仍有既有學習文章未註冊標籤警告，本輪未修改。
- RSS 已核對桌機、390px、320px 實際外觀；本機預覽已關閉。

## 暫緩

推薦方向保留為文章底部延伸閱讀更貼近當前主題。本輪未改推薦演算法，保留前輪 related_posts、recommendation_topics、標籤／分類及穩定排序。熱力圖日期、手機圖例及空白提示字型保留前輪成果。
