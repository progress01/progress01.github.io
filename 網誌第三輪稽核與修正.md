# 網誌第三輪稽核與修正

日期：2026-09-10～2026-09-11。由主代理檢查、審閱及驗收，LUNA 修正網站程式與六份產文 SKILL。本輪已完成本機修正與技能安裝，未 commit、push 或部署。

| 檢查方向 | 確認的問題 | 本輪處理 |
| --- | --- | --- |
| 網站與頁面身分 | 首頁第 2 頁也輸出 WebSite，分類頁名稱只有站名 | 僅真正首頁為 WebSite；分頁、分類、標籤、年月彙整為 CollectionPage，名稱包含各頁資訊 |
| 文章與網頁身分 | BlogPosting 與 mainEntityOfPage 共用相同 ID | 文章 ID 使用 canonical 加上 `#article`，網頁仍指向 canonical，另引用首頁 `#website` |
| 更新日期 | 243 篇中有 223 篇沒有 updated，原先回退到檔案修改時間 | `updated_option: date`；沒有明確更新日期就使用發布日期，明確 updated 繼續優先，避免 checkout／轉檔使日期漂移 |
| JSON-LD 輸出 | JSON 序列化沒有處理 HTML script 結束標記 | 增加共用安全序列化 helper，將 `<` 等字元轉成 Unicode escape；含 `</script>` 的標題仍可完整解析 |
| 驗證能力 | 原本只檢查 JSON 語法；合法多行 YAML 會誤判、不存在的日期會通過 | 使用真正 YAML parser；新增 title、日期、時間、時區、updated 順序及 cover 路徑檢查；JSON-LD 檢查唯一性、canonical、類型、ID、日期及圖片 |
| 草稿夾資料 | topics/items 缺少資料形狀、唯一 ID 與 state 防呆 | 檢查必要欄位、有效日期、ID 重複、state 列舉、完成日期順序及 `/learning/` 路徑 |
| 標籤來源 | 主標籤註冊檔與硬編碼別名分開，新增註冊項目仍可能被剔除 | canonical 與 aliases 統一在 `source/_data/content-tags.yml`，normalizer／檢查器共用；每次生成重新載入，避免長時間開啟 server 使用舊規則 |
| 舊文章標籤 | 兩篇歌曲 tags 空白、welcome-board 沒有 tags | 分別補上音樂推薦及站務，僅修改 metadata，保留正文和日期 |
| 產文技能 | 驗證指令、cover、圖牆更新方式與批次工具範圍不一致 | 六份 SKILL 統一 verify；歌曲補 cover／alt、更新卡片去重；明確區分單篇修補與整站工具，保留既有寫作偏好 |

## 驗收結果

- `npm run verify` 通過：22 項測試、937 個產出檔案、327 個 HTML、32,036 個 href/src 引用。
- 全站 JSON-LD：1 個 WebSite、243 個 BlogPosting、77 個 CollectionPage、6 個 WebPage。
- 獨立比對 243 篇文章的原始 front matter 與產出 dateModified：223 篇日期回退、20 篇明確 updated，零不一致。文章可見發布日期與 OG 更新日期也與 JSON-LD 一致。
- 5 個一般內容分類、9 個主標籤、212 張圖牆卡片的圖片引用通過檢查。
- 回歸案例包含 script 結束字串、錯誤 JSON-LD 類型／null、非法日期、多行分類、閏日、同時刻混合時區、壞 URI、重複 ID、未知 state、標籤重新載入及 prototype 名稱。
- `moment-timezone` 已明確列入 package.json 與 lock 的根依賴；宣告、lock 與已安裝版本一致。
- `git diff --check` 通過。
- 開始前既有的五個修改檔案已以 SHA-256 前後比對，全部保持原樣：排球攻擊揮臂機制、reading-desk.yml、工作知識－簡報不是功能清單、值得的等待、有些事情要分開。

這些 JSON-LD 檢查是本站的資料一致性規則，不代表 Google 將每個欄位都列為必填，也不保證搜尋結果外觀。Google 的 Article 文件將適用欄位列為建議；網站名稱的 WebSite 資料則應描述首頁。[Article 官方文件](https://developers.google.com/search/docs/appearance/structured-data/article)、[網站名稱官方文件](https://developers.google.com/search/docs/appearance/site-names)。本輪未使用線上 Rich Results Test 或 Search Console 驗證尚未部署的版本。

## 已安裝的 SKILL

主代理使用完整 PyYAML 6.0.3，對六份已安裝技能重新執行官方 `quick_validate.py`，全部通過；安裝後逐份比對與 LUNA 修訂檔案的雜湊一致。只更新 SKILL.md，保留各技能的 agents 與其他資源。

- [歌曲推薦](C:/Users/111/.codex/skills/song-blog-update/SKILL.md)：保留原模板、IG 轉載句與文字解析偏好；加入 cover、主標籤、alt、更新既有卡片及單篇圖片處理規則。
- [Blogger 匯入](C:/Users/111/.codex/skills/blogger-import-update/SKILL.md)：全形／半形來源標記、feed 範圍確認、避免覆蓋本地改稿及誤用整站工具。
- [生活索引](C:/Users/111/.codex/skills/life-index-update/SKILL.md)：修正 calendar 產物位置，統一驗證流程。
- [N+天日記](C:/Users/111/.codex/skills/n-plus-days-blog-update/SKILL.md)：保留摘要長度與持續更新方式，加入分類／日期一致性規則。
- [草稿夾與學習筆記](C:/Users/111/.codex/skills/reading-desk-update/SKILL.md)：穩定 ID、固定 state、文章與清單 URL 一致；保留每日新增及永久保存的語意。
- [工作知識](C:/Users/111/.codex/skills/work-knowledge-blog-update/SKILL.md)：保留去情境化與系列視覺，對齊主標籤及更新日期規則。

舊版備份位置：`C:\Users\111\AppData\Local\Temp\codex-blog-skills-backup-3cc0c48befad447eaa31a03d25d8edf7`。此為暫存目錄，長期保留應另行備份。

## 後續可調整處

- 109 篇舊歌曲文章沒有 `cover`。頁面正文圖片正常，但 JSON-LD 不會包含 image。本輪先修正新產文規則；舊文可再依實際封面補齊，避免把任意第一張圖直接當成封面。
- 11 篇文章含 20 種未註冊的技術標籤。原始文字保留、主標籤正常；建置現在明確列出忽略警告。若要把這些詞變成新的瀏覽分類，應先決定標籤設計，再更新註冊檔。
- Blogger 舊批次工具仍有全站掃描範圍；本輪以 SKILL 限制誤用。後續可為工具加入單篇目標選項，並進一步統一來源標記與 metadata parser。
- 未發現重複的明確 permalink 或 PNG/JPG 轉 WebP 的同名目標衝突；圖牆目前沒有 PNG/JPG 活躍引用。這些不能被當成已發生的錯誤。

日常內容更新仍執行 `npm run verify`；新增實質內容時，填入可信的 updated，保留原始 date 與 permalink。
