# 推薦與熱力圖設計稽核

日期：2026-09-12。範圍：目前工作區原始碼、已安裝推薦套件及本地 public 產出。以下「優先處理」與「需要釐清的設計」是修正前盤點；本文件末節另記錄本輪已完成的文件與文案同步，不表示目前實作仍停留在修正前狀態。這份文件不取代最終驗收結果。

## 優先處理

### 1. 文章推薦過度集中（推薦品質）

- 目前 111 篇帶「音樂推薦」標籤的文章，其推薦欄合計只推薦到 6 篇；其中 5 篇各出現在 110 個推薦欄。
- 17 篇「學習筆記」也只推薦到 6 篇，其中 5 篇各出現 16 次。這是本地產出的曝光位置數，不是訪客點閱數。
- 現行路徑為 `themes/next/layout/_macro/post.njk:172` → `scripts/popular-posts-fixed.js:2` → 已安裝套件 `lib/list-json.js`。helper 傳空選項，預設按共同標籤數排序；未啟用內容分析，熱門混合比例為 0。同分且 PV 相同時沒有額外輪替規則，沿用候選順序。
- `source/_data/content-tags.yml` 把 ACID、GEO、CMS、排球等整併成「學習筆記」，導覽變簡潔，但共同標籤已不足以分辨題材。此盤點未作整併前後對照，不能把全部集中現象歸因於最近整併。
- 建議保留九個主標籤，另設可選的推薦關聯資料：作者指定相關文章優先，再依細部主題／同系列配對，最後用同分類補足；同分採可重現的分散排序。不要以純亂數取代相關性，也不要為此增加訪客追蹤。

### 2. 修正前的推薦設定盤點（歷史問題）

- 修正前曾把根目錄設定、外部套件選項與自訂 helper 混為一談，無法從文件判定 `limit` 是否真的控制文章底部推薦。
- 本輪核對後，現行 `scripts/popular-posts-fixed.js` 已直接讀取 `_config.yml:148` 的 `popular_posts.enable` 與 `popular_posts.limit`，由 helper 自行建立候選與推薦結果；文章可用 `related_posts` 與 `recommendation_topics` 提供選擇依據。
- 本節的舊問題只作為修正背景；維護時應依現行 helper 與手冊介面操作，不再使用已淘汰的外部套件參數說法。

### 3. 文章「近一週」少一天（日期錯誤）

- `source/calendar/index.md:75` 先把今天設到 23:59:59.999，再減六天作起點；文章日期是當日午夜，因此排除了起點那一天。
- 固定今天為 2026-09-12、每天各一篇，實際只回傳 09-07～09-12，漏掉應納入的 09-06。
- 建議起點設為六天前 00:00，終點為今天結束，加入跨月／跨年邊界驗證。

### 4. 閏年少了 12/31 的日期按鈕（操作錯誤）

- `source/calendar/index.md:163` 用 `new Date(year, 1, 0)` 判斷閏年，但它取得的是一月最後一天，永遠是 31。
- 實際呼叫 2024 年按鈕產生函式，只有 365 顆，最後日期是 12/30。此證據指覆蓋層／鍵盤日期按鈕，不代表底層 ECharts 沒有畫出 12/31。
- 建議用正確年長計算，並核對 365／366 天與最後日期。草稿夾目前沒有使用這個錯誤公式。

### 5. 草稿夾把五題顯示成四題（數字不一致）

- `source/reading/index.md:126` 在資料層使用 `Math.min(4, count)`，再把該值傳給圖表提示。
- 2026-09-04 的實際資料有 5 題，圖表 formatter 卻輸出「4 個當日新增題目」；日期按鈕及明細保留真實數量。
- 建議保留原始數量，只讓色階在 4 以上飽和，圖例標示「4+」。色階分級不應截斷提示數字。

### 6. 草稿夾把已過日期標示為「尚未到達」（日期語意錯誤）

- `source/reading/index.md:171` 以最後新增題目的日期判斷 future，而非今天；點擊處理也用同一界線。
- 目前最後新增日期為 09-08。在 09-12 檢查時，09-09 的日期按鈕仍標示「尚未到達」，且不註冊點擊。
- 建議區分「今天以後」與「沒有新增紀錄」。若希望圖表只呈現到資料截止日，應清楚標示資料截止，不能稱其後所有日期為未來。

## 需要釐清的設計

### 7. 文章熱力圖其實是發表紀錄

- `scripts/calendar-generator.js:26`、`:41` 皆按 `post.date` 計數；畫面卻稱「更新」「異動」。修訂舊文章不會增加修訂日的格子。
- 建議保留發表歷史並改為「文章發表紀錄」；若要呈現維護活動，可另加最近修訂清單。
- `updated` 只有最後修訂日期，不能重建全部修訂事件；直接改用它，也會讓文章離開原本發表日。完整修改歷史需另外保存事件。
- 草稿夾則維持「當日新增題目」，已完成題目仍保留原始新增日，不改回未完成數累加。

### 8. 隨機時光機採分類均衡，建議保留

- `source/random/index.md:228` 先排除最近三次抽到的文章，再從仍有候選的分類平均抽一類，最後抽文章。
- 目前分類篇數：音樂 111、閱讀與影視 105、觀念與實驗 17、生活紀錄 6、工作知識 4。歷史為空且各類皆有候選時，每類機率為 20%；不是每篇文章等機率。
- 這能避免數量大的分類占滿抽取結果，是合理取捨。它不是根據閱讀偏好學習的個人化推薦。
- 「最近三篇不重複」遇到分類不足四篇且候選耗盡時，程式會回退允許重複（`:245`）。目前五類皆至少四篇，這是資料量變少時的邊界；文案宜交代候選不足的例外。

## 驗證與範圍

- `node tmp/audit-recommendation-output.js`：逐頁讀取目前 random.json 對應的 243 個文章 HTML，統計推薦曝光；未發現缺少對應 HTML 或推薦自身。
- 文章熱力圖兩份 JSON 合計均為 245 篇，逐日計數與明細一致。以上是當次本地快照，未驗證正式站是否部署同一版本。
- `node tmp/audit-calendar-boundaries.js`：執行實際頁面函式，重現六天窗口與閏年缺按鈕。
- `node tmp/audit-reading-count.js`：執行實際頁面與 Calendar 函式，重現 5→4 提示數字及已過日期被當作未來。
- 未執行瀏覽器視覺驗收；未宣稱手機點擊位置或版面完全正確。後續修正需驗證桌機／手機、鍵盤選日與圖表點擊一致。
- 建議順序：先修日期與數字錯誤，再處理推薦集中與無效設定，最後同步介面文案及維護規範。

## 本輪文件與文案修正

- 維護手冊已把文章熱力圖與最近文章明確說明為依發表 `date` 產生；`updated` 只代表修訂時間，不另造熱力圖事件。
- 隨機傳送門保留既有分類均衡程式，只補充最近三次不重複及候選耗盡時允許重抽的介面文案。
- 維護手冊已同步目前實際推薦流程：`popular_posts.enable/limit` 已接線，helper 依 `related_posts`（站內相對路徑）與可選 `recommendation_topics` 選擇推薦，並以 tags/categories 補足；未新增 `about`、`sameAs` 等未接線欄位。
